version: '3.8'
services:
  mcp-command-server:
    build: .
    container_name: mcp-command-server
    ports:
      - "8080:8080"
    restart: unless-stopped
    # Optional: add network to internal-only Traefik zone
    # networks:
    #   - internal
    # security_opt:
    #   - no-new-privileges:true